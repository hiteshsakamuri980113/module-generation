Assumptions:
- Created project folder name: react-vite-tailwind
- Using recent major versions for dependencies; run `npm install` to fetch exact versions.

Next steps: run `npm install` then `npm run dev` to start development server.
